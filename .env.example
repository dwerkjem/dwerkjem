# Copy to .env and fill in values
# GitHub Personal Access Token with repo scope to include private repos via API
GITHUB_TOKEN=ghp_your_pat_here
# Set to 1 to compute stats via SSH clones (requires working SSH auth)
USE_GIT_SSH=0
